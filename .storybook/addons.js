import '@storybook/addon-actions/register';
import '@storybook/addon-links/register';

// import '../src/addons';

// try {
//   const reqAddons = require.context('../addons', true, /register\.js$/);
//   reqAddons.keys().forEach(filename => reqAddons(filename));
// } catch(err) {
//   console.log(err);
// }
